"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ParallelTransform_1 = __importDefault(require("./ParallelTransform"));
function pipe(asyncTransformFn, opts = {}) {
    const onTransformFn = function (data, callback) {
        try {
            const returned = asyncTransformFn.call(this, data);
            Promise.resolve(returned).then((resolved) => callback(undefined, resolved));
        }
        catch (e) {
            callback(e);
        }
    };
    const options = typeof opts === 'number'
        ? { maxParallel: opts }
        : opts;
    return new ParallelTransform_1.default(onTransformFn, options);
}
exports.default = pipe;
